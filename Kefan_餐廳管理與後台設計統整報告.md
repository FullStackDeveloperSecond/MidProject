---

## 本次修改

- 後台整體視覺改版：`design-system.css` 色彩／字體 token 全面換成暖色系（品牌橘、暖白底、柔和狀態色），卡片圓角與陰影一併調整，Dashboard／會員一覽／檢舉管理／通知管理／點數商城／餐廳管理共用套用
- 補齊兩個原本未套用共用設計系統的頁面（`Reports/Details`、`Notifications/Create`＋`Edit`），改用 ds- 元件呈現，視覺風格與其他頁面一致
- 側邊欄底色維持原本深藍，頂部 Topbar 完全未變動

- 餐廳一覽新增「表格／卡片」檢視切換（前端純顯示切換，選擇記在 localStorage）
- 停用餐廳一覽頂部提示改為警示橫幅樣式；解除停用改為二次確認彈窗，避免手滑誤觸
- 餐廳標籤一覽卡片改為色塊視覺呈現（依標籤名稱穩定分配 8 色）
- 點數商城（外框商品／兌換紀錄）版面整合進共用設計系統，含分頁資料
- 餐廳／標籤管理加上 `[Authorize(Roles = "Admin")]` 保護，並記錄實際操作管理員 ID
- 移除舊版圖片上傳系統，待後續由 Alex 重新設計上傳流程

- 評論管理／檢舉管理支援依 `RestaurantID` 精確篩選
- 修正瀏覽器 bfcache 導致的彈跳視窗殘留問題
- 修正餐廳星等／評論數改為即時計算

## 對應 Issue
Closes #(請填入對應的 Issue 編號)

## 測試方式
1. 以管理員帳號登入後台，依序瀏覽全部模組頁面，確認版面風格一致、無跑版
2. 「餐廳一覽」點擊「表格／卡片」切換鈕，確認畫面正確切換，重整後仍記得選擇
3. 「停用餐廳一覽」任一筆詳細頁點擊「解除停用」，確認先跳確認彈窗，取消不變更、確認後成功恢復
4. 「餐廳標籤一覽」確認色塊顏色穩定顯示，新增／停用／啟用功能正常
5. 以非 Admin 角色嘗試訪問 `/Restaurants`、`/Tags`，確認被導向登入或拒絕
6. 從餐廳詳情頁點「查看評論」「查看檢舉」，確認只顯示該餐廳資料
7. 新增評論後，比對詳情頁與列表頁星等/評論數即時一致
8. 新增/編輯餐廳後按瀏覽器上一頁，確認無殘留彈跳視窗

## 是否修改資料庫 / Model
- [ ] 是　- [x] 否
（僅新增/調整 ViewModel，無 migration、無資料表變更）

## 是否影響其他模組
- [x] 是　- [ ] 否
（視覺調整影響全部模組頁面；權限與 RestaurantID 篩選涉及餐廳/評論/檢舉三模組）

## 備註
- 圖片上傳系統已移除，餐廳圖片表單暫時空缺，待 Alex 重新設計
- `AccountController.Login` 未讀取 ReturnUrl，屬既有問題非本次範圍

---

Issue 編號那格幫你留空了，麻煩你補上對應的號碼。