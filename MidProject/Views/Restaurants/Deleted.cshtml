@model MidProject.Models.ViewModels.Restaurants.RestaurantDeletedIndexViewModel
@{
    ViewData["Title"] = "停用餐廳一覽";
}

@section Styles {
    <link rel="stylesheet" href="~/css/restaurant-admin.css" asp-append-version="true" />
}

<div class="ra-page">
    <div class="ra-topbar">
        <h1>停用餐廳一覽</h1>
    </div>

    @if (TempData["Toast"] != null)
    {
        <div class="alert">@TempData["Toast"]</div>
    }

    <div class="alert">以下餐廳已停用（IsDeleted = 1），一般用戶不可見。點擊進入詳細頁面可編輯或解除停用。</div>

    <div class="stats-grid">
        <article class="stat-card"><span>停用總數</span><strong>@Model.StatsDisabledTotal</strong></article>
        <article class="stat-card"><span>最近停用</span><strong>@(Model.StatsMostRecentAt?.ToString("yyyy-MM-dd") ?? "-")</strong><small>@(Model.StatsMostRecentName ?? "-")</small></article>
        <article class="stat-card"><span>前次停用原因</span><strong>@(Model.StatsLastReason ?? "-")</strong></article>
    </div>

    <form method="get" asp-action="Deleted" class="toolbar" id="filterForm">
        <div class="filter-row deleted-filters">
            <input class="input" type="search" name="Search" value="@Model.Filter.Search" placeholder="搜尋停用餐廳...">
            <select class="select" name="City" onchange="this.form.requestSubmit()">
                <option value="">全部縣市</option>
                @foreach (var city in Model.AvailableCities)
                {
                    <option value="@city" selected="@(city == Model.Filter.City)">@city</option>
                }
            </select>
            <select class="select" name="Reason" onchange="this.form.requestSubmit()">
                <option value="">全部停用原因</option>
                @foreach (var reason in Model.AvailableReasons)
                {
                    <option value="@reason" selected="@(reason == Model.Filter.Reason)">@reason</option>
                }
            </select>
            <button type="submit" class="btn">搜尋</button>
        </div>
    </form>

    <div class="table-wrap">
        <table>
            <thead>
                <tr>
                    <th>餐廳名稱</th>
                    <th>縣市 / 行政區</th>
                    <th>標籤</th>
                    <th>停用時間</th>
                    <th>停用原因</th>
                    <th>執行者</th>
                    <th>操作</th>
                </tr>
            </thead>
            <tbody>
                @if (!Model.Items.Any())
                {
                    <tr><td colspan="7" class="muted">目前沒有停用餐廳。</td></tr>
                }
                else
                {
                    foreach (var item in Model.Items)
                    {
                        <tr class="ra-row">
                            <td><strong>@item.Name</strong></td>
                            <td>@item.City · @item.District</td>
                            <td>
                                @foreach (var tag in item.Tags)
                                {
                                    <span class="tag neutral">@tag</span>
                                }
                            </td>
                            <td class="muted">@(item.DeletedAt?.ToString("yyyy-MM-dd") ?? "-")</td>
                            <td><span class="tag violation">@item.DeleteReason</span></td>
                            <td class="muted">@item.DeletedByName</td>
                            <td>
                                <a class="btn btn-icon" title="查看" aria-label="查看" href="@Url.Action("Details", new { id = item.Id })">
                                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M1 12s4-7 11-7 11 7 11 7-4 7-11 7-11-7-11-7Z" /><circle cx="12" cy="12" r="3" /></svg>
                                </a>
                                <button type="button" class="btn btn-icon js-edit-btn" title="編輯" aria-label="編輯" data-id="@item.Id">
                                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 20h9" /><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4Z" /></svg>
                                </button>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>

<div id="modalRoot" class="modal-root" aria-hidden="true"></div>
<div id="toast" class="toast" role="status"></div>
<div id="pageLoadingOverlay" class="page-loading-overlay" aria-hidden="true">
    <div class="page-loading-content">
        <div class="page-loading-spinner"></div>
        <p>載入中... 請稍後</p>
    </div>
</div>

@section Scripts {
    <script src="~/js/restaurant-admin.js" asp-append-version="true"></script>
    <script>
        document.querySelectorAll('.js-edit-btn').forEach(btn => {
            btn.addEventListener('click', () => RestaurantAdmin.openEditModal(btn.dataset.id));
        });
        RestaurantAdmin.bindPageLoadingOverlay();
    </script>
}
